package config;

/**
 * @author LZG
 */
public class CommonConstants {

    /**
     * rts_type 返回的类型
     */
    public final static String RETURN_TO_SCALE = "VRS";

    /**
     * ORIENTATION 默认是input，也可能是both，both目前不支持
     */
    public final static String ORIENTATION = "input";
}
